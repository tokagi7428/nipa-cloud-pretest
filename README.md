# nipa-cloud-pretest

# Enter to api
$ cd server

# install dependencies
$ npm install
# run server
$ npm start

# Enter to Front end
$ cd frontend

# install depen
# install dependencies
$ npm install
